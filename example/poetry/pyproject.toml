[tool.poetry]
name = "example"
version = "0.1.0"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
numpy = "^2.0"
pyarrow = "^18.0.0"
cowsay = "^6.1"
colorama = "^0.4.6"
polars = "^0.20.30"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
