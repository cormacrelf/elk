[project]
name = "app-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["lib-common", "numpy>=2.0"]

[tool.uv.sources]
lib-common = { workspace = true }

[build-system]
requires = ["uv_build>=0.11.4,<0.12"]
build-backend = "uv_build"
